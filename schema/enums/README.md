# Schema — Enums

Index of custom enum types in the public schema. Full values are in [reference/enums.md](../../reference/enums.md).

## Enum types (from Supabase)

active_inactive, admin_ticket_impact_enum, admin_ticket_status_enum, admin_ticket_sub_department_enum, annual_leave_types, biomap_stage, biomap_stage2, cardio_timetrial_tests, coach_notes_type, gender, good_bad_enum, gym, health_rating, journey_stage_type, member_health_enum, membership_category_enum, membership_duration_months_enum, membership_status, membership_term, month_enum, onboarding_phases, payment_status, pipelinelost_churnmarker, programming_stage, roll_call_dependency, sale_objection_reason, schedule_block_enum, schedule_preference_type, session_bracket_enum, sop_categories, sprint_period_enum, staff_roles, status_enum, stripe_invoice_status_enum, task_size, teambuildr_completion_enum, tod_category_enum, weekdays, yes_no_enum

## Usage

Use these in schema table docs and reference/enums.md when documenting column types. reference/enums.md is refreshed from Supabase (see MAINTENANCE.md).
