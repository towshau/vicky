# Supporting tables

Tables that are not in the core set are listed in [schema/_index.md](../_index.md) with category "supporting". Add individual docs here as needed (e.g. when a rule or SOP references the table). Use the [schema table template](../../_templates/schema-table.md).

No supporting table docs yet.
